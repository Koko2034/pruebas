import { ConfigEditor, Config as StrykerConfig } from 'stryker-api/config';
export default class KarmaConfigEditor implements ConfigEditor {
    edit(strykerConfig: StrykerConfig): void;
    private static importFiles(strykerConfig, karmaConfig);
    private static importDefaultKarmaConfig(strykerConfig, karmaConfig);
    private static toInputFileDescriptor(karmaPattern);
}
